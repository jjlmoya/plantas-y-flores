---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="Colaboraciones - Plantas y Flores | Trabajemos juntos"
  description="¿Eres una marca que comparte nuestros valores de jardinería sostenible y responsable? Descubre cómo podemos colaborar para crear contenido auténtico que inspire a nuestra comunidad de jardineros."
  type="website"
  canonical="https://plantasyflores.online/colaboraciones/"
  keywords="colaboraciones, partnerships, marcas jardinería, contenido plantas, jardinería sostenible, influencer plantas"
  publishedDate="2025-01-01T00:00:00.000Z"
  modifiedDate={new Date().toISOString()}
  image="/brand/logo-200.webp"
>
  <div class="partnerships-page">
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-content">
        <div class="hero-text">
          <h1 class="hero-title">Trabajemos Juntos</h1>
          <p class="hero-subtitle">Creando contenido auténtico para una jardinería más consciente</p>
        </div>
        <div class="hero-image">
          <img 
            src="/brand/logo-200.webp" 
            alt="Jardineramable - Plantas y Flores" 
            class="brand-logo"
            width="150"
            height="150"
          >
        </div>
      </div>
    </section>

    <!-- About Section -->
    <section class="about-section">
      <div class="container">
        <h2>Sobre Jardineramable</h2>
        <div class="about-grid">
          <div class="about-text">
            <p>Hola, soy <strong>jardineramable</strong>, la persona detrás de Plantas y Flores. Durante años he construido una comunidad apasionada por la jardinería responsable y sostenible.</p>
            
            <p>Mi enfoque no es solo mostrar plantas bonitas, sino educar sobre el cuidado responsable, la sostenibilidad y el respeto por la naturaleza. Cada colaboración que realizo debe alinearse con estos valores fundamentales.</p>
            
            <div class="stats">
              <div class="stat">
                <span class="stat-number">113+</span>
                <span class="stat-label">Artículos de plantas</span>
              </div>
              <div class="stat">
                <span class="stat-number">24</span>
                <span class="stat-label">Categorías especializadas</span>
              </div>
              <div class="stat">
                <span class="stat-number">5+</span>
                <span class="stat-label">Años de experiencia</span>
              </div>
            </div>
          </div>
          
          <div class="values-card">
            <h3>Nuestros Valores</h3>
            <ul class="values-list">
              <li>🌱 <strong>Sostenibilidad</strong> - Prácticas respetuosas con el medio ambiente</li>
              <li>📚 <strong>Educación</strong> - Contenido que realmente ayude a aprender</li>
              <li>🤝 <strong>Autenticidad</strong> - Solo productos que uso y recomiendo genuinamente</li>
              <li>🌍 <strong>Responsabilidad</strong> - Promover una jardinería consciente</li>
              <li>💚 <strong>Comunidad</strong> - Fomentar el intercambio de conocimiento</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- Services Section -->
    <section class="services-section">
      <div class="container">
        <h2>Tipos de Colaboración</h2>
        <div class="services-grid">
          <div class="service-card">
            <div class="service-icon">📝</div>
            <h3>Artículos Especializados</h3>
            <p>Creación de contenido en profundidad sobre productos o técnicas, siempre con enfoque educativo y experiencia personal.</p>
            <ul>
              <li>Guías detalladas de uso</li>
              <li>Comparativas honestas</li>
              <li>Casos de estudio reales</li>
            </ul>
          </div>

          <div class="service-card">
            <h3>Reviews Auténticas</h3>
            <div class="service-icon">⭐</div>
            <p>Análisis exhaustivos de productos después de uso prolongado, destacando tanto fortalezas como áreas de mejora.</p>
            <ul>
              <li>Pruebas durante temporadas completas</li>
              <li>Feedback honesto y constructivo</li>
              <li>Seguimiento de resultados</li>
            </ul>
          </div>

          <div class="service-card">
            <div class="service-icon">🎥</div>
            <h3>Contenido Visual</h3>
            <p>Fotografías de alta calidad y contenido visual que muestre productos en uso real en jardines y hogares.</p>
            <ul>
              <li>Fotografía de producto en contexto</li>
              <li>Antes y después de tratamientos</li>
              <li>Procesos paso a paso</li>
            </ul>
          </div>

          <div class="service-card">
            <div class="service-icon">🌐</div>
            <h3>Integración Web</h3>
            <p>Incorporación natural de productos en nuestras guías y herramientas, manteniendo la utilidad para el usuario.</p>
            <ul>
              <li>Recomendaciones en artículos</li>
              <li>Inclusión en herramientas de cálculo</li>
              <li>Menciones en consejos estacionales</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- Ideal Partners Section -->
    <section class="partners-section">
      <div class="container">
        <h2>Marcas Ideales para Colaborar</h2>
        <div class="partners-content">
          <div class="partners-text">
            <p>Busco colaborar con marcas que compartan mi visión de una jardinería responsable y sostenible. Si tu empresa se identifica con estos valores, me encantaría conocerte:</p>
          </div>
          
          <div class="partners-grid">
            <div class="partner-type">
              <h3>🌱 Productos Ecológicos</h3>
              <ul>
                <li>Fertilizantes orgánicos y naturales</li>
                <li>Sustratos sostenibles</li>
                <li>Semillas ecológicas y autóctonas</li>
                <li>Tratamientos respetuosos con el medio ambiente</li>
              </ul>
            </div>

            <div class="partner-type">
              <h3>🔧 Herramientas de Calidad</h3>
              <ul>
                <li>Herramientas duraderas y ergonómicas</li>
                <li>Sistemas de riego eficientes</li>
                <li>Macetas y contenedores sostenibles</li>
                <li>Tecnología que facilite el cuidado</li>
              </ul>
            </div>

            <div class="partner-type">
              <h3>🏡 Vida Sostenible</h3>
              <ul>
                <li>Productos para huertos urbanos</li>
                <li>Soluciones para espacios pequeños</li>
                <li>Decoración natural para hogares</li>
                <li>Productos que fomenten la biodiversidad</li>
              </ul>
            </div>

            <div class="partner-type">
              <h3>📚 Educación y Formación</h3>
              <ul>
                <li>Cursos y talleres de jardinería</li>
                <li>Libros y recursos educativos</li>
                <li>Apps y herramientas digitales</li>
                <li>Programas de certificación</li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Process Section -->
    <section class="process-section">
      <div class="container">
        <h2>Cómo Trabajamos</h2>
        <div class="process-steps">
          <div class="step">
            <div class="step-number">1</div>
            <h3>Primer Contacto</h3>
            <p>Conversación inicial para conocer tu marca, productos y objetivos. Evaluamos la alineación con nuestros valores.</p>
          </div>
          
          <div class="step">
            <div class="step-number">2</div>
            <h3>Propuesta Personalizada</h3>
            <p>Desarrollo de una propuesta específica que beneficie tanto a tu marca como a nuestra comunidad.</p>
          </div>
          
          <div class="step">
            <div class="step-number">3</div>
            <h3>Período de Prueba</h3>
            <p>Uso real del producto durante el tiempo necesario para formar una opinión honesta y documentada.</p>
          </div>
          
          <div class="step">
            <div class="step-number">4</div>
            <h3>Creación de Contenido</h3>
            <p>Desarrollo de contenido auténtico basado en experiencia real, manteniendo siempre la honestidad.</p>
          </div>
          
          <div class="step">
            <div class="step-number">5</div>
            <h3>Publicación y Seguimiento</h3>
            <p>Publicación del contenido y seguimiento de resultados, con feedback constructivo para futuras colaboraciones.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section class="contact-section">
      <div class="container">
        <h2>Empecemos a Colaborar</h2>
        <div class="contact-content">
          <div class="contact-info">
            <h3>¿Listo para una colaboración auténtica?</h3>
            <p>Si tu marca comparte nuestros valores de sostenibilidad, educación y respeto por la naturaleza, me encantaría conocer más sobre tus productos y cómo podemos trabajar juntos.</p>
            
            <div class="contact-guidelines">
              <h4>Antes de contactar, ten en cuenta:</h4>
              <ul>
                <li>✅ Solo colaboro con productos que uso personalmente</li>
                <li>✅ Mantengo siempre la honestidad en mis recomendaciones</li>
                <li>✅ Priorizo marcas con valores sostenibles</li>
                <li>✅ El contenido siempre debe aportar valor educativo</li>
                <li>❌ No acepto colaboraciones que contradigan mis valores</li>
              </ul>
            </div>
          </div>
          
          <div class="contact-form-container">
            <form class="contact-form" id="colaboraciones-form">
              <h3>Formulario de Contacto</h3>
              
              <!-- Mensaje de estado -->
              <div id="form-status" class="form-status" style="display: none;"></div>
              
              <div class="form-group">
                <label for="company">Nombre de la Empresa *</label>
                <input type="text" id="company" name="company" required>
              </div>
              
              <div class="form-row">
                <div class="form-group">
                  <label for="name">Tu Nombre *</label>
                  <input type="text" id="name" name="name" required>
                </div>
                
                <div class="form-group">
                  <label for="email">Email *</label>
                  <input type="email" id="email" name="email" required>
                </div>
              </div>
              
              <div class="form-group">
                <label for="website">Sitio Web de la Empresa</label>
                <input type="text" id="website" name="website" placeholder="https://ejemplo.com">
              </div>
              
              <div class="form-group">
                <label for="products">¿Qué productos o servicios ofrecéis? *</label>
                <textarea id="products" name="products" rows="3" required></textarea>
              </div>
              
              <div class="form-group">
                <label for="collaboration">Tipo de Colaboración que Buscáis</label>
                <select id="collaboration" name="collaboration">
                  <option value="">Selecciona una opción</option>
                  <option value="article">Artículo especializado</option>
                  <option value="review">Review de producto</option>
                  <option value="visual">Contenido visual</option>
                  <option value="integration">Integración web</option>
                  <option value="other">Otro (especificar en mensaje)</option>
                </select>
              </div>
              
              <div class="form-group">
                <label for="values">¿Cómo se alinean vuestros valores con la sostenibilidad y jardinería responsable? *</label>
                <textarea id="values" name="values" rows="4" required placeholder="Contadnos sobre vuestro compromiso con el medio ambiente, prácticas sostenibles, responsabilidad social..."></textarea>
              </div>
              
              <div class="form-group">
                <label for="message">Mensaje Adicional</label>
                <textarea id="message" name="message" rows="4" placeholder="Contadnos más detalles sobre vuestra propuesta, objetivos, timeline..."></textarea>
              </div>
              
              <div class="form-group checkbox-group">
                <label class="checkbox-label">
                  <input type="checkbox" name="privacy" required>
                  <span class="checkmark"></span>
                  He leído y acepto que mis datos sean utilizados para responder a esta consulta de colaboración
                </label>
              </div>
              
              <button type="submit" class="submit-btn" id="submit-btn">
                <span class="btn-text">Enviar Propuesta</span>
                <span class="btn-loading" style="display: none;">Enviando...</span>
              </button>
            </form>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer CTA -->
    <section class="footer-cta">
      <div class="container">
        <div class="cta-content">
          <h2>Juntos podemos inspirar una jardinería más consciente</h2>
          <p>Si tu marca tiene una historia que contar y productos que realmente marcan la diferencia, hablemos.</p>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
.partnerships-page {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0;
  background: #fafbfc;
  overflow-x: hidden;
}

/* Hero Section */
.hero {
  background: linear-gradient(135deg, #2d5016 0%, #4a7c23 100%);
  color: white;
  padding: 0;
  margin-bottom: 0;
  border: none;
  border-radius: 0;
  position: relative;
  overflow: hidden;
  min-height: 70vh;
  display: flex;
  align-items: center;
}

.hero::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url("data:image/svg+xml,%3Csvg width='40' height='40' viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='rgba(255,255,255,0.03)' fill-rule='evenodd'%3E%3Cpath d='m0 40 40-40H20L0 20M40 40V20l-20 20'/%3E%3C/g%3E%3C/svg%3E");
  animation: float 20s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  50% { transform: translateY(-10px) rotate(1deg); }
}

.hero-content {
  display: grid;
  grid-template-columns: 1fr 300px;
  align-items: center;
  max-width: 1200px;
  margin: 0 auto;
  gap: 4rem;
  padding: 4rem 2rem;
  position: relative;
  z-index: 2;
}

.hero-text {
  padding-right: 2rem;
}

.hero-title {
  font-size: 4.5rem;
  font-weight: 900;
  color: white;
  margin: 0 0 1.5rem 0;
  line-height: 1;
  background: linear-gradient(135deg, #ffffff 0%, #e8f5e8 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
}

.hero-subtitle {
  font-size: 1.375rem;
  color: rgba(255, 255, 255, 0.9);
  margin: 0;
  font-weight: 400;
  line-height: 1.6;
  max-width: 500px;
}

.hero-image {
  display: flex;
  justify-content: center;
  align-items: center;
  position: relative;
}

.brand-logo {
  width: 200px;
  height: 200px;
  filter: drop-shadow(0 20px 40px rgba(0, 0, 0, 0.3));
  animation: pulse 3s ease-in-out infinite;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  padding: 2rem;
  backdrop-filter: blur(10px);
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

/* About Section */
.about-section {
  background: white;
  padding: 6rem 0;
  margin-bottom: 0;
  position: relative;
}

.about-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, transparent, #4a7c23, transparent);
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem;
}

.about-section h2 {
  font-size: 3rem;
  font-weight: 800;
  color: #2d5016;
  text-align: center;
  margin: 0 0 1rem 0;
  position: relative;
}

.about-section h2::after {
  content: '';
  display: block;
  width: 80px;
  height: 4px;
  background: linear-gradient(90deg, #4a7c23, #8bc34a);
  margin: 1rem auto 3rem;
  border-radius: 2px;
}

.about-grid {
  display: grid;
  grid-template-columns: 1.5fr 1fr;
  gap: 4rem;
  align-items: start;
}

.about-text {
  font-size: 1.25rem;
  line-height: 1.8;
  color: #2c3e50;
}

.about-text p {
  margin-bottom: 2rem;
}

.about-text strong {
  color: #2d5016;
  font-weight: 700;
}

.stats {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1.5rem;
  margin-top: 3rem;
}

.stat {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 1.5rem 2rem;
  background: linear-gradient(135deg, #f8fffe 0%, #e8f5f0 100%);
  border-radius: 16px;
  box-shadow: 0 4px 20px rgba(74, 124, 35, 0.1);
  border: 1px solid rgba(74, 124, 35, 0.1);
  transition: transform 0.3s ease;
}

.stat:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 30px rgba(74, 124, 35, 0.15);
}

.stat-number {
  font-size: 2.5rem;
  font-weight: 900;
  color: #4a7c23;
  line-height: 1;
  min-width: 80px;
}

.stat-label {
  font-size: 1rem;
  color: #2d5016;
  font-weight: 600;
  line-height: 1.3;
}

.values-card {
  background: linear-gradient(135deg, #2d5016 0%, #4a7c23 100%);
  color: white;
  padding: 3rem;
  border-radius: 24px;
  box-shadow: 0 20px 40px rgba(45, 80, 22, 0.3);
  position: relative;
  overflow: hidden;
}

.values-card::before {
  content: '';
  position: absolute;
  top: -50%;
  right: -50%;
  width: 100%;
  height: 100%;
  background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
  animation: shimmer 4s ease-in-out infinite;
}

@keyframes shimmer {
  0%, 100% { transform: rotate(0deg); }
  50% { transform: rotate(180deg); }
}

.values-card h3 {
  font-size: 1.75rem;
  font-weight: 800;
  color: white;
  margin: 0 0 2rem 0;
  position: relative;
  z-index: 2;
}

.values-list {
  list-style: none;
  padding: 0;
  margin: 0;
  position: relative;
  z-index: 2;
}

.values-list li {
  margin-bottom: 1.25rem;
  font-size: 1.125rem;
  line-height: 1.6;
  color: rgba(255, 255, 255, 0.95);
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
}

.values-list li:last-child {
  margin-bottom: 0;
}

/* Services Section */
.services-section {
  background: linear-gradient(135deg, #fafbfc 0%, #f0f4f7 100%);
  padding: 6rem 0;
  margin-bottom: 0;
  position: relative;
}

.services-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 200px;
  background: linear-gradient(180deg, white 0%, transparent 100%);
  pointer-events: none;
}

.services-section h2 {
  font-size: 3rem;
  font-weight: 800;
  color: #2d5016;
  text-align: center;
  margin: 0 0 1rem 0;
  position: relative;
  z-index: 2;
}

.services-section h2::after {
  content: '';
  display: block;
  width: 80px;
  height: 4px;
  background: linear-gradient(90deg, #4a7c23, #8bc34a);
  margin: 1rem auto 4rem;
  border-radius: 2px;
}

.services-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 3rem;
  position: relative;
  z-index: 2;
}

.service-card {
  background: white;
  padding: 3rem;
  border-radius: 24px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08);
  text-align: left;
  transition: all 0.4s ease;
  border: 1px solid rgba(74, 124, 35, 0.1);
  position: relative;
  overflow: hidden;
}

.service-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, #4a7c23, #8bc34a);
  transform: scaleX(0);
  transition: transform 0.4s ease;
}

.service-card:hover::before {
  transform: scaleX(1);
}

.service-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 20px 60px rgba(74, 124, 35, 0.15);
}

.service-icon {
  font-size: 3.5rem;
  margin-bottom: 1.5rem;
  display: block;
}

.service-card h3 {
  font-size: 1.5rem;
  font-weight: 800;
  color: #2d5016;
  margin: 0 0 1.5rem 0;
  line-height: 1.3;
}

.service-card p {
  color: #555;
  line-height: 1.7;
  margin-bottom: 2rem;
  font-size: 1.125rem;
}

.service-card ul {
  list-style: none;
  padding: 0;
}

.service-card li {
  color: #666;
  margin-bottom: 0.75rem;
  position: relative;
  padding-left: 1.5rem;
  font-size: 1rem;
  line-height: 1.5;
}

.service-card li::before {
  content: '✓';
  color: #4a7c23;
  font-weight: bold;
  position: absolute;
  left: 0;
  top: 0;
}

/* Partners Section */
.partners-section {
  background: white;
  padding: 6rem 0;
  margin-bottom: 0;
  position: relative;
}

.partners-section h2 {
  font-size: 3rem;
  font-weight: 800;
  color: #2d5016;
  text-align: center;
  margin: 0 0 1rem 0;
}

.partners-section h2::after {
  content: '';
  display: block;
  width: 80px;
  height: 4px;
  background: linear-gradient(90deg, #4a7c23, #8bc34a);
  margin: 1rem auto 2rem;
  border-radius: 2px;
}

.partners-text {
  text-align: center;
  font-size: 1.25rem;
  color: #2c3e50;
  margin-bottom: 4rem;
  max-width: 700px;
  margin-left: auto;
  margin-right: auto;
  line-height: 1.7;
}

.partners-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 3rem;
}

.partner-type {
  background: linear-gradient(135deg, #f8fffe 0%, #e8f5f0 100%);
  padding: 3rem;
  border-radius: 24px;
  box-shadow: 0 10px 40px rgba(74, 124, 35, 0.1);
  border: 1px solid rgba(74, 124, 35, 0.1);
  transition: all 0.4s ease;
  position: relative;
  overflow: hidden;
}

.partner-type::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, #4a7c23, #8bc34a);
}

.partner-type:hover {
  transform: translateY(-4px);
  box-shadow: 0 20px 60px rgba(74, 124, 35, 0.2);
}

.partner-type h3 {
  font-size: 1.5rem;
  font-weight: 800;
  color: #2d5016;
  margin: 0 0 2rem 0;
  display: flex;
  align-items: center;
  gap: 0.75rem;
}

.partner-type ul {
  list-style: none;
  padding: 0;
}

.partner-type li {
  color: #555;
  margin-bottom: 1rem;
  position: relative;
  padding-left: 1.5rem;
  font-size: 1.125rem;
  line-height: 1.5;
}

.partner-type li::before {
  content: '✓';
  color: #4a7c23;
  font-weight: bold;
  position: absolute;
  left: 0;
  top: 0;
}

/* Process Section */
.process-section {
  background: linear-gradient(135deg, #2d5016 0%, #1a2f0a 100%);
  color: white;
  padding: 6rem 0;
  margin-bottom: 0;
  position: relative;
  overflow: hidden;
}

.process-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.03'%3E%3Ccircle cx='30' cy='30' r='3'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
}

.process-section h2 {
  font-size: 3rem;
  font-weight: 800;
  text-align: center;
  margin: 0 0 1rem 0;
  position: relative;
  z-index: 2;
}

.process-section h2::after {
  content: '';
  display: block;
  width: 80px;
  height: 4px;
  background: linear-gradient(90deg, #8bc34a, #ffffff);
  margin: 1rem auto 4rem;
  border-radius: 2px;
}

.process-steps {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 2rem;
  position: relative;
  z-index: 2;
}

.step {
  text-align: center;
  padding: 2rem 1rem;
  position: relative;
}

.step::after {
  content: '';
  position: absolute;
  top: 50%;
  right: -1rem;
  width: 2rem;
  height: 2px;
  background: linear-gradient(90deg, rgba(255,255,255,0.3), transparent);
  transform: translateY(-50%);
}

.step:last-child::after {
  display: none;
}

.step-number {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 80px;
  height: 80px;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.2), rgba(255, 255, 255, 0.1));
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-radius: 50%;
  font-size: 1.75rem;
  font-weight: 900;
  margin-bottom: 1.5rem;
  backdrop-filter: blur(10px);
  transition: all 0.3s ease;
}

.step:hover .step-number {
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.3), rgba(255, 255, 255, 0.2));
  transform: scale(1.1);
}

.step h3 {
  font-size: 1.375rem;
  font-weight: 800;
  margin: 0 0 1rem 0;
  line-height: 1.3;
}

.step p {
  font-size: 1rem;
  line-height: 1.6;
  opacity: 0.9;
  color: rgba(255, 255, 255, 0.95);
}

/* Contact Section */
.contact-section {
  background: linear-gradient(135deg, #fafbfc 0%, #f0f4f7 100%);
  padding: 6rem 0;
  margin-bottom: 0;
}

.contact-section h2 {
  font-size: 3rem;
  font-weight: 800;
  color: #2d5016;
  text-align: center;
  margin: 0 0 1rem 0;
}

.contact-section h2::after {
  content: '';
  display: block;
  width: 80px;
  height: 4px;
  background: linear-gradient(90deg, #4a7c23, #8bc34a);
  margin: 1rem auto 4rem;
  border-radius: 2px;
}

.contact-content {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: start;
}

.contact-info h3 {
  font-size: 2rem;
  font-weight: 800;
  color: #2d5016;
  margin: 0 0 1.5rem 0;
  line-height: 1.3;
}

.contact-info p {
  font-size: 1.25rem;
  color: #2c3e50;
  line-height: 1.7;
  margin-bottom: 2.5rem;
}

.contact-guidelines {
  background: linear-gradient(135deg, #2d5016 0%, #4a7c23 100%);
  color: white;
  padding: 2.5rem;
  border-radius: 20px;
  box-shadow: 0 20px 40px rgba(45, 80, 22, 0.3);
  position: relative;
  overflow: hidden;
}

.contact-guidelines::before {
  content: '';
  position: absolute;
  top: -50%;
  right: -50%;
  width: 100%;
  height: 100%;
  background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
  animation: shimmer 4s ease-in-out infinite;
}

.contact-guidelines h4 {
  font-size: 1.375rem;
  font-weight: 800;
  color: white;
  margin: 0 0 1.5rem 0;
  position: relative;
  z-index: 2;
}

.contact-guidelines ul {
  list-style: none;
  padding: 0;
  margin: 0;
  position: relative;
  z-index: 2;
}

.contact-guidelines li {
  margin-bottom: 0.75rem;
  font-size: 1rem;
  color: rgba(255, 255, 255, 0.95);
  line-height: 1.5;
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
}

/* Form Styles */
.contact-form-container {
  background: white;
  padding: 3rem;
  border-radius: 24px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
  border: 1px solid rgba(74, 124, 35, 0.1);
  position: relative;
  overflow: hidden;
  width: 100%;
  box-sizing: border-box;
}

.contact-form-container::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, #4a7c23, #8bc34a);
}

.contact-form h3 {
  font-size: 2rem;
  font-weight: 800;
  color: #2d5016;
  margin: 0 0 2.5rem 0;
  text-align: center;
}

.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.5rem;
}

.form-group {
  margin-bottom: 2rem;
}

.form-group label {
  display: block;
  font-weight: 700;
  color: #2d5016;
  margin-bottom: 0.75rem;
  font-size: 1rem;
}

.form-group input,
.form-group select,
.form-group textarea {
  width: 100%;
  padding: 1.125rem 1.25rem;
  border: 2px solid #e5e7eb;
  border-radius: 12px;
  font-size: 1rem;
  transition: all 0.3s ease;
  font-family: inherit;
  background: #fafbfc;
  box-sizing: border-box;
}

.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
  outline: none;
  border-color: #4a7c23;
  background: white;
  transform: translateY(-1px);
  box-shadow: 0 4px 20px rgba(74, 124, 35, 0.1);
}

.form-group textarea {
  resize: vertical;
  min-height: 120px;
}

.checkbox-group {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
}

.checkbox-label {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  font-size: 1rem;
  line-height: 1.6;
  color: #555;
  cursor: pointer;
}

.checkbox-label input[type="checkbox"] {
  width: auto;
  margin: 0;
  margin-top: 0.25rem;
  transform: scale(1.2);
}

.submit-btn {
  width: 100%;
  background: linear-gradient(135deg, #4a7c23 0%, #2d5016 100%);
  color: white;
  padding: 1.25rem 2rem;
  border: none;
  border-radius: 16px;
  font-size: 1.25rem;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.4s ease;
  box-shadow: 0 4px 20px rgba(74, 124, 35, 0.3);
  position: relative;
  overflow: hidden;
}

.submit-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
  transition: left 0.6s;
}

.submit-btn:hover::before {
  left: 100%;
}

.submit-btn:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 30px rgba(74, 124, 35, 0.4);
}

/* Footer CTA */
.footer-cta {
  background: linear-gradient(135deg, #2d5016 0%, #1a2f0a 100%);
  color: white;
  padding: 4rem 2rem;
  text-align: center;
  position: relative;
  overflow: hidden;
}

.footer-cta::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url("data:image/svg+xml,%3Csvg width='40' height='40' viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='rgba(255,255,255,0.03)' fill-rule='evenodd'%3E%3Cpath d='m0 40 40-40H20L0 20M40 40V20l-20 20'/%3E%3C/g%3E%3C/svg%3E");
  animation: float 20s ease-in-out infinite;
}

.cta-content {
  position: relative;
  z-index: 2;
}

.cta-content h2 {
  font-size: 2.5rem;
  font-weight: 800;
  color: white;
  margin: 0 0 1.5rem 0;
  line-height: 1.3;
}

.cta-content p {
  font-size: 1.25rem;
  color: rgba(255, 255, 255, 0.9);
  margin: 0;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
  line-height: 1.6;
}

/* Mobile Styles */
@media (max-width: 768px) {
  .hero {
    min-height: 60vh;
  }
  
  .hero-content {
    grid-template-columns: 1fr;
    text-align: center;
    gap: 2rem;
    padding: 3rem 1rem;
  }
  
  .hero-text {
    padding-right: 0;
  }
  
  .hero-title {
    font-size: 2.5rem;
  }
  
  .hero-subtitle {
    font-size: 1.125rem;
    max-width: none;
  }
  
  .brand-logo {
    width: 120px;
    height: 120px;
  }
  
  .about-grid {
    grid-template-columns: 1fr;
    gap: 3rem;
  }
  
  .stats {
    grid-template-columns: 1fr;
  }
  
  .services-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  
  .partners-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  
  .process-steps {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  
  .step::after {
    display: none;
  }
  
  .contact-content {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  
  .form-row {
    grid-template-columns: 1fr;
    gap: 1rem;
  }
  
  .container {
    padding: 0 1rem;
  }
  
  .about-section,
  .services-section,
  .partners-section,
  .process-section,
  .contact-section {
    padding: 3rem 0;
  }
  
  .contact-form-container {
    padding: 2rem 1.5rem;
    margin: 0 1rem;
  }
  
  .form-group {
    margin-bottom: 1.5rem;
  }
  
  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: 1rem;
    font-size: 16px; /* Prevents zoom on iOS */
  }
}

@media (max-width: 480px) {
  .hero {
    min-height: 50vh;
  }
  
  .hero-content {
    padding: 2rem 0.75rem;
    gap: 1.5rem;
  }
  
  .hero-title {
    font-size: 2rem;
    line-height: 1.2;
  }
  
  .hero-subtitle {
    font-size: 1rem;
  }
  
  .brand-logo {
    width: 100px;
    height: 100px;
    padding: 1.5rem;
  }
  
  .about-section h2,
  .services-section h2,
  .partners-section h2,
  .process-section h2,
  .contact-section h2 {
    font-size: 2rem;
  }
  
  .contact-form-container {
    padding: 1.5rem;
    margin: 0 0.5rem;
    border-radius: 16px;
  }
  
  .contact-form h3 {
    font-size: 1.5rem;
  }
  
  .form-group {
    margin-bottom: 1.25rem;
  }
  
  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: 0.875rem;
    border-radius: 8px;
  }
  
  .submit-btn {
    padding: 1rem 1.5rem;
    font-size: 1.125rem;
  }
  
  .service-card,
  .partner-type {
    padding: 1.5rem;
  }
  
  .values-card,
  .contact-guidelines {
    padding: 1.5rem;
  }
  
  .container {
    padding: 0 0.75rem;
  }
  
  .about-section,
  .services-section,
  .partners-section,
  .process-section,
  .contact-section {
    padding: 2.5rem 0;
  }
}

/* Form Status Styles */
.form-status {
  padding: 1rem;
  border-radius: 8px;
  margin-bottom: 1.5rem;
  font-weight: 600;
  text-align: center;
}

.form-status.success {
  background: #d4edda;
  color: #155724;
  border: 1px solid #c3e6cb;
}

.form-status.error {
  background: #f8d7da;
  color: #721c24;
  border: 1px solid #f5c6cb;
}

.btn-loading {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
}

.btn-loading::after {
  content: '';
  width: 16px;
  height: 16px;
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-radius: 50%;
  border-top-color: white;
  animation: spin 1s ease-in-out infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.submit-btn:disabled {
  opacity: 0.7;
  cursor: not-allowed;
  transform: none !important;
}
</style>

<script>
// URL de tu Google Apps Script Web App
const GOOGLE_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbyMu-D1v4aC3FEGykT4ooBeJie1EdM3Lr4aUq0F3HsNwQhea90TdwrDUsNQXCWkehCr/exec';

document.addEventListener('DOMContentLoaded', function() {
  const form = document.getElementById('colaboraciones-form');
  const submitBtn = document.getElementById('submit-btn');
  const btnText = submitBtn.querySelector('.btn-text');
  const btnLoading = submitBtn.querySelector('.btn-loading');
  const formStatus = document.getElementById('form-status');

  form.addEventListener('submit', async function(e) {
    e.preventDefault();
    console.log('Formulario enviado - iniciando proceso...');
    
    // Validar campos requeridos manualmente
    const requiredFields = form.querySelectorAll('[required]');
    let hasErrors = false;
    
    requiredFields.forEach(field => {
      if (!field.value.trim()) {
        field.style.borderColor = '#dc3545';
        hasErrors = true;
        console.log('Campo requerido vacío:', field.name);
      } else {
        field.style.borderColor = '#e5e7eb';
      }
    });
    
    if (hasErrors) {
      showError('Por favor, completa todos los campos obligatorios marcados con *');
      return;
    }
    
    // Mostrar estado de carga
    showLoading();
    console.log('Iniciando envío a Google Sheets...');
    
    // Recopilar datos del formulario
    const formData = new FormData(form);
    const data = Object.fromEntries(formData.entries());
    
    // Añadir timestamp
    data.timestamp = new Date().toLocaleString('es-ES', {
      timeZone: 'Europe/Madrid',
      year: 'numeric',
      month: '2-digit',
      day: '2-digit',
      hour: '2-digit',
      minute: '2-digit'
    });
    
    console.log('Datos a enviar:', data);
    
    try {
      console.log('Enviando a:', GOOGLE_SCRIPT_URL);
      
      // Intentar con URL params (método más compatible)
      const params = new URLSearchParams();
      Object.keys(data).forEach(key => {
        params.append(key, data[key]);
      });

      console.log('Parámetros a enviar:', params.toString());

      const response = await fetch(GOOGLE_SCRIPT_URL, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/x-www-form-urlencoded',
        },
        body: params.toString(),
        mode: 'no-cors'
      });
      
      console.log('Enviado con URLSearchParams');
      
      // Con no-cors no podemos leer la respuesta, asumimos éxito
      showSuccess('¡Gracias! Tu propuesta de colaboración ha sido enviada correctamente. Te contactaremos pronto.');
      form.reset();
    } catch (error) {
      console.error('Error completo:', error);
      showError('Hubo un problema al enviar tu mensaje. Error: ' + error.message + '. Por favor, inténtalo de nuevo.');
    } finally {
      hideLoading();
    }
  });
  
  function showLoading() {
    submitBtn.disabled = true;
    btnText.style.display = 'none';
    btnLoading.style.display = 'inline-flex';
    formStatus.style.display = 'none';
  }
  
  function hideLoading() {
    submitBtn.disabled = false;
    btnText.style.display = 'inline';
    btnLoading.style.display = 'none';
  }
  
  function showSuccess(message) {
    formStatus.textContent = message;
    formStatus.className = 'form-status success';
    formStatus.style.display = 'block';
    formStatus.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  }
  
  function showError(message) {
    formStatus.textContent = message;
    formStatus.className = 'form-status error';
    formStatus.style.display = 'block';
    formStatus.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  }
});
</script>