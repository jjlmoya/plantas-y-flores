---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="Colaboraciones - Plantas y Flores | Trabajemos juntos"
  description="¿Eres una marca que comparte nuestros valores de jardinería sostenible y responsable? Descubre cómo podemos colaborar para crear contenido auténtico que inspire a nuestra comunidad de jardineros."
  type="website"
  canonical="https://plantasyflores.online/colaboraciones/"
  keywords="colaboraciones, partnerships, marcas jardinería, contenido plantas, jardinería sostenible, influencer plantas"
  publishedDate="2025-01-01T00:00:00.000Z"
  modifiedDate={new Date().toISOString()}
  image="/brand/logo-200.webp"
  noAds={true}
>
  <div class="partnerships-page">
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-content">
        <div class="hero-image">
          <img 
            src="/icons/jardinera-amable/feliz-approve.png" 
            alt="Jardineramable - Lista para colaborar" 
            class="jardinera-avatar"
            width="140"
            height="140"
          >
        </div>
        <div class="hero-text">
          <h1 class="hero-title">Colabora conmigo para llegar a amantes de las plantas con confianza real</h1>
          <p class="hero-subtitle">Contenido útil, honesto y probado en la práctica. Sin publicidad disfrazada.</p>
          <a href="#contact-form" class="primary-cta">Proponer colaboración</a>
        </div>
      </div>
    </section>

    <!-- Authority Block -->
    <section class="authority-section">
      <div class="container">
        <div class="authority-stats">
          <div class="stat-item">
            <div class="stat-icon">🌱</div>
            <div class="stat-content">
              <span class="stat-number">5 años</span>
              <span class="stat-label">escribiendo sobre jardinería</span>
            </div>
          </div>
          <div class="stat-item">
            <div class="stat-icon">📚</div>
            <div class="stat-content">
              <span class="stat-number">113 artículos</span>
              <span class="stat-label">publicados</span>
            </div>
          </div>
          <div class="stat-item">
            <div class="stat-icon">👥</div>
            <div class="stat-content">
              <span class="stat-number">Comunidad</span>
              <span class="stat-label">fiel y activa</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Collaboration Formats Section -->
    <section class="formats-section">
      <div class="container">
        <h2>Formatos de colaboración</h2>
        <div class="formats-grid">
          <div class="format-card">
            <div class="format-icon">📝</div>
            <h3>Artículo patrocinado</h3>
            <p>Contenido práctico y probado con fotos propias.</p>
            <a href="#contact-form" class="secondary-cta"><span>Solicitar este formato</span></a>
          </div>

          <div class="format-card">
            <div class="format-icon">⭐</div>
            <h3>Reseña en profundidad</h3>
            <p>Pros y contras tras uso real del producto.</p>
            <a href="#contact-form" class="secondary-cta"><span>Solicitar este formato</span></a>
          </div>

          <div class="format-card">
            <div class="format-icon">📚</div>
            <h3>Guía temática con integración</h3>
            <p>Artículo educativo con producto integrado naturalmente.</p>
            <a href="#contact-form" class="secondary-cta"><span>Solicitar este formato</span></a>
          </div>
        </div>
      </div>
    </section>

    <!-- Examples Section -->
    <section class="examples-section">
      <div class="container">
        <h2>Ejemplos de nuestro trabajo</h2>
        <div class="examples-grid">
          <div class="example-card stagger-child">
            <img src="/wp-content/uploads/2018/06/rosa-333187_640.webp" alt="Artículo sobre Rosa Princesa de Mónaco" class="example-image" loading="lazy">
            <div class="example-content">
              <h3>Rosa Princesa de Mónaco</h3>
              <p>Rosa de té híbrida con tonos blancos y rosas, resistente y de gran elegancia para jardines señoriales.</p>
              <a href="/rosa/princesa-de-monaco/" class="example-link">Leer artículo completo</a>
            </div>
          </div>

          <div class="example-card stagger-child">
            <img src="/wp-content/uploads/2018/01/basil-2541581_1920-325x244.webp" alt="Artículo sobre Albahaca Morada" class="example-image" loading="lazy">
            <div class="example-content">
              <h3>Albahaca Morada</h3>
              <p>Variedad híbrida con hojas dentadas de tonos morados intensos, ideal para vinagres y aceites aromáticos.</p>
              <a href="/albahaca/morada/" class="example-link">Leer artículo completo</a>
            </div>
          </div>

          <div class="example-card stagger-child">
            <img src="/wp-content/uploads/2017/07/cherry-tomatoes-2152341_1280-300x200.webp" alt="Artículo sobre Tomate Cherry" class="example-image" loading="lazy">
            <div class="example-content">
              <h3>Tomate Cherry: De la semilla al fruto</h3>
              <p>Seguimiento completo de temporada con técnicas probadas y resultados reales en huerto urbano.</p>
              <a href="/tomate/cherry/" class="example-link">Leer artículo completo</a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Process Section -->
    <section class="process-section">
      <div class="container">
        <h2>Proceso de trabajo</h2>
        <div class="process-steps">
          <div class="step stagger-child">
            <div class="step-icon">🎯</div>
            <h3>Objetivo</h3>
            <p>Definimos juntos los objetivos y alineamos la colaboración con los valores de la comunidad.</p>
          </div>
          
          <div class="step stagger-child">
            <div class="step-icon">🧪</div>
            <h3>Prueba del producto</h3>
            <p>Uso real durante el tiempo necesario para formar una opinión honesta y documentada.</p>
          </div>
          
          <div class="step stagger-child">
            <div class="step-icon">✍️</div>
            <h3>Borrador</h3>
            <p>Creación de contenido auténtico basado en experiencia práctica, manteniendo total honestidad.</p>
          </div>
          
          <div class="step stagger-child">
            <div class="step-icon">📢</div>
            <h3>Publicación y seguimiento</h3>
            <p>Publicación del contenido con seguimiento de resultados y feedback para futuras colaboraciones.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Collaboration Values Section -->
    <section class="values-section">
      <div class="container">
        <h2>Valores de colaboración</h2>
        <div class="values-grid">
          <div class="value-item stagger-child">
            <div class="value-icon">✅</div>
            <div class="value-content">
              <h3>Transparencia</h3>
              <p>Siempre identifico colaboraciones</p>
            </div>
          </div>
          
          <div class="value-item stagger-child">
            <div class="value-icon">✅</div>
            <div class="value-content">
              <h3>Honestidad</h3>
              <p>No recomiendo lo que no usaría</p>
            </div>
          </div>
          
          <div class="value-item stagger-child">
            <div class="value-icon">✅</div>
            <div class="value-content">
              <h3>Responsabilidad</h3>
              <p>Cuido tanto el contenido como a la audiencia</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq-section">
      <div class="container">
        <h2>Preguntas frecuentes</h2>
        <div class="faq-list">
          <div class="faq-item stagger-child">
            <button class="faq-question" aria-expanded="false">
              <span>¿Aceptas guiones cerrados?</span>
              <span class="faq-toggle">+</span>
            </button>
            <div class="faq-answer">
              <p>No, adapto a mi estilo y experiencia.</p>
            </div>
          </div>
          
          <div class="faq-item stagger-child">
            <button class="faq-question" aria-expanded="false">
              <span>¿Puedo revisar antes de publicar?</span>
              <span class="faq-toggle">+</span>
            </button>
            <div class="faq-answer">
              <p>Sí, recibirás borrador.</p>
            </div>
          </div>
          
          <div class="faq-item stagger-child">
            <button class="faq-question" aria-expanded="false">
              <span>¿Cuánto tardas?</span>
              <span class="faq-toggle">+</span>
            </button>
            <div class="faq-answer">
              <p>Normalmente entre 7 y 14 días.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section class="contact-section" id="contact-form">
      <div class="container">
        <h2>Formulario de contacto</h2>
        <div class="contact-content">
          <div class="contact-info">
            <p>¿Interesado en colaborar? Cuéntame sobre tu marca y cómo podemos trabajar juntos para crear contenido valioso para la comunidad.</p>
            <div class="direct-contact">
              <h3>Email directo:</h3>
              <a href="mailto:jardineraamable@gmail.com" class="email-link">jardineraamable@gmail.com</a>
            </div>
          </div>
          
          <div class="contact-form-container">
            <form class="contact-form" id="colaboraciones-form">
              <!-- Mensaje de estado -->
              <div id="form-status" class="form-status" style="display: none;"></div>
              
              <div class="form-group">
                <label for="name">Nombre *</label>
                <input type="text" id="name" name="name" required>
              </div>
              
              <div class="form-group">
                <label for="company">Empresa *</label>
                <input type="text" id="company" name="company" required>
              </div>
              
              <div class="form-group">
                <label for="email">Email *</label>
                <input type="email" id="email" name="email" required>
              </div>
              
              <div class="form-group">
                <label for="collaboration">Objetivo</label>
                <select id="collaboration" name="collaboration">
                  <option value="">Selecciona el tipo de colaboración</option>
                  <option value="articulo">Artículo patrocinado</option>
                  <option value="reseña">Reseña en profundidad</option>
                  <option value="guia">Guía temática</option>
                  <option value="otro">Otro</option>
                </select>
              </div>
              
              <div class="form-group">
                <label for="message">Mensaje *</label>
                <textarea id="message" name="message" rows="5" required placeholder="Cuéntame sobre tu propuesta, productos, objetivos y cómo podemos colaborar..."></textarea>
              </div>
              
              <button type="submit" class="submit-btn" id="submit-btn">
                <span class="btn-text">Enviar propuesta</span>
                <span class="btn-loading" style="display: none;">Enviando...</span>
              </button>
            </form>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer CTA -->
    <section class="footer-cta">
      <div class="container">
        <div class="cta-content">
          <h2>Juntos podemos inspirar una jardinería más consciente</h2>
          <p>Si tu marca tiene una historia que contar y productos que realmente marcan la diferencia, hablemos.</p>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
.partnerships-page {
  max-width: 1400px;
  margin: 0 auto;
  padding: 0;
  background: #fafbfc;
  overflow-x: hidden;
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem;
}

/* Hero Section */
.hero {
  background: linear-gradient(135deg, #e8f5f0 0%, #f8fffe 100%);
  color: #2d5016;
  padding: 4rem 0;
  margin-bottom: 0;
  border: none;
  border-radius: 0;
  position: relative;
  overflow: hidden;
  opacity: 0;
  animation: fadeInUp 1.2s ease-out forwards;
}

.hero::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url("data:image/svg+xml,%3Csvg width='40' height='40' viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='rgba(255,255,255,0.03)' fill-rule='evenodd'%3E%3Cpath d='m0 40 40-40H20L0 20M40 40V20l-20 20'/%3E%3C/g%3E%3C/svg%3E");
  animation: float 20s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  50% { transform: translateY(-10px) rotate(1deg); }
}

.hero-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  max-width: 1200px;
  margin: 0 auto;
  gap: 2rem;
  padding: 0 2rem;
  position: relative;
  z-index: 2;
}

.hero-text {
  max-width: 650px;
}

.hero-title {
  font-size: 3rem;
  font-weight: 800;
  color: #2d5016;
  margin: 0 0 1.5rem 0;
  line-height: 1.3;
  opacity: 0;
  transform: translateY(30px);
  animation: slideInUp 1s ease-out 0.3s forwards;
}

.hero-subtitle {
  font-size: 1.25rem;
  color: #4a7c23;
  margin: 0 0 2.5rem 0;
  font-weight: 400;
  line-height: 1.6;
  opacity: 0;
  transform: translateY(20px);
  animation: slideInUp 1s ease-out 0.6s forwards;
}

.primary-cta {
  display: inline-block;
  background: #4a7c23;
  color: white;
  padding: 1rem 2rem;
  font-size: 1.125rem;
  font-weight: 700;
  text-decoration: none;
  border-radius: 12px;
  transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  box-shadow: 0 4px 20px rgba(74, 124, 35, 0.3);
  min-height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 180px;
  opacity: 0;
  transform: translateY(20px) scale(0.9);
  animation: slideInScale 1s ease-out 0.9s forwards;
  position: relative;
  overflow: hidden;
}

.primary-cta::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
  transition: left 0.6s ease;
}

.primary-cta:hover::before {
  left: 100%;
}

.primary-cta:hover {
  background: #2d5016;
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 12px 40px rgba(74, 124, 35, 0.5);
}

.primary-cta:focus {
  outline: 3px solid #4a7c23;
  outline-offset: 2px;
}

.secondary-cta {
  display: inline-block;
  background: transparent;
  color: #4a7c23;
  padding: 0.75rem 1.5rem;
  font-size: 1rem;
  font-weight: 600;
  text-decoration: none;
  border: 2px solid #4a7c23;
  border-radius: 8px;
  transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  text-align: center;
  min-height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

.secondary-cta::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 0;
  height: 100%;
  background: #4a7c23;
  transition: width 0.3s ease;
  z-index: 0;
}

.secondary-cta:hover::before {
  width: 100%;
}

.secondary-cta:hover {
  color: white;
  border-color: #4a7c23;
  transform: translateY(-2px) scale(1.05);
  box-shadow: 0 6px 20px rgba(74, 124, 35, 0.3);
}

.secondary-cta span {
  position: relative;
  z-index: 1;
}

.secondary-cta:focus {
  outline: 3px solid #4a7c23;
  outline-offset: 2px;
}

.hero-image {
  display: flex;
  justify-content: center;
  align-items: center;
  position: relative;
}

.jardinera-avatar {
  width: 140px;
  height: 140px;
  filter: drop-shadow(0 4px 20px rgba(74, 124, 35, 0.2));
  animation: gentle-bounce 4s ease-in-out infinite;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.8);
  padding: 1.5rem;
  margin-bottom: 1rem;
  opacity: 0;
  transform: scale(0.8);
  animation: gentle-bounce 4s ease-in-out infinite, fadeInScale 1s ease-out 0.1s forwards;
}

@keyframes gentle-bounce {
  0%, 100% { transform: translateY(0px) scale(1); }
  50% { transform: translateY(-5px) scale(1.02); }
}

/* Authority Section */
.authority-section {
  background: white;
  padding: 4rem 0;
  margin-bottom: 0;
  opacity: 0;
  animation: fadeInUp 1s ease-out forwards;
  animation-delay: 0.5s;
}

.authority-stats {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}

.stat-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 2rem;
  background: linear-gradient(135deg, #f8fffe 0%, #e8f5f0 100%);
  border-radius: 16px;
  box-shadow: 0 4px 20px rgba(74, 124, 35, 0.1);
  border: 1px solid rgba(74, 124, 35, 0.1);
  transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  text-align: left;
  opacity: 0;
  transform: translateY(30px);
  position: relative;
  overflow: hidden;
}

.stat-item::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
  transition: left 0.8s ease;
}

.stat-item:hover::before {
  left: 100%;
}

.stat-item:hover {
  transform: translateY(-6px) scale(1.02);
  box-shadow: 0 12px 40px rgba(74, 124, 35, 0.2);
}

.stat-item:nth-child(1) {
  animation: slideInUp 0.8s ease-out 0.7s forwards;
}

.stat-item:nth-child(2) {
  animation: slideInUp 0.8s ease-out 0.9s forwards;
}

.stat-item:nth-child(3) {
  animation: slideInUp 0.8s ease-out 1.1s forwards;
}

.stat-icon {
  font-size: 2.5rem;
  min-width: 60px;
  transition: transform 0.3s ease;
}

.stat-item:hover .stat-icon {
  transform: scale(1.1) rotate(5deg);
}

.stat-content {
  flex: 1;
}

.stat-number {
  display: block;
  font-size: 1.5rem;
  font-weight: 800;
  color: #2d5016;
  line-height: 1.2;
  margin-bottom: 0.25rem;
}

.stat-label {
  display: block;
  font-size: 1rem;
  color: #666;
  font-weight: 500;
  line-height: 1.3;
}

/* Formats Section */
.formats-section {
  background: linear-gradient(135deg, #fafbfc 0%, #f0f4f7 100%);
  padding: 4rem 0;
  margin-bottom: 0;
  opacity: 0;
  animation: fadeInUp 1s ease-out forwards;
  animation-delay: 0.8s;
}

.formats-section h2 {
  font-size: 2.5rem;
  font-weight: 800;
  color: #2d5016;
  text-align: center;
  margin: 0 0 3rem 0;
}

.formats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}

.format-card {
  background: white;
  padding: 2rem;
  border-radius: 16px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
  text-align: center;
  transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  border: 1px solid rgba(74, 124, 35, 0.1);
  position: relative;
  overflow: hidden;
  opacity: 0;
  transform: translateY(30px);
}

.format-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, #4a7c23, #8bc34a);
  transform: scaleX(0);
  transition: transform 0.3s ease;
}

.format-card:hover::before {
  transform: scaleX(1);
}

.format-card:hover {
  transform: translateY(-8px) scale(1.03);
  box-shadow: 0 15px 50px rgba(74, 124, 35, 0.2);
}

.format-card:nth-child(1) {
  animation: slideInUp 0.8s ease-out 1.2s forwards;
}

.format-card:nth-child(2) {
  animation: slideInUp 0.8s ease-out 1.4s forwards;
}

.format-card:nth-child(3) {
  animation: slideInUp 0.8s ease-out 1.6s forwards;
}

.format-icon {
  font-size: 3rem;
  margin-bottom: 1.5rem;
  display: block;
  transition: transform 0.3s ease;
}

.format-card:hover .format-icon {
  transform: scale(1.2) rotate(-10deg);
  animation: pulse 2s ease-in-out infinite;
}

.format-card h3 {
  font-size: 1.375rem;
  font-weight: 700;
  color: #2d5016;
  margin: 0 0 1rem 0;
  line-height: 1.3;
}

.format-card p {
  color: #666;
  line-height: 1.6;
  margin-bottom: 1.5rem;
  font-size: 1rem;
}

/* Examples Section */
.examples-section {
  background: white;
  padding: 4rem 0;
  margin-bottom: 0;
  opacity: 0;
  transform: translateY(40px);
  transition: all 1s ease-out;
}

.examples-section.animate-in {
  opacity: 1;
  transform: translateY(0);
}

.examples-section h2 {
  font-size: 2.5rem;
  font-weight: 800;
  color: #2d5016;
  text-align: center;
  margin: 0 0 3rem 0;
}

.examples-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}

.example-card {
  background: white;
  border-radius: 16px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
  overflow: hidden;
  transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  border: 1px solid rgba(74, 124, 35, 0.1);
  opacity: 0;
  transform: translateY(30px);
}

.example-card.stagger-child.animate-in {
  opacity: 1;
  transform: translateY(0);
}

.example-card:hover {
  transform: translateY(-6px) scale(1.03);
  box-shadow: 0 15px 50px rgba(74, 124, 35, 0.2);
}

.example-card:hover .example-image {
  transform: scale(1.05);
}

.example-image {
  width: 100%;
  height: 200px;
  object-fit: cover;
  border-bottom: 1px solid #f0f4f7;
  transition: transform 0.4s ease;
}

.example-content {
  padding: 1.5rem;
}

.example-content h3 {
  font-size: 1.25rem;
  font-weight: 700;
  color: #2d5016;
  margin: 0 0 0.75rem 0;
  line-height: 1.3;
}

.example-content p {
  color: #666;
  line-height: 1.6;
  margin-bottom: 1rem;
  font-size: 0.95rem;
}

.example-link {
  color: #4a7c23;
  text-decoration: none;
  font-weight: 600;
  font-size: 0.9rem;
  transition: color 0.3s ease;
}

.example-link:hover {
  color: #2d5016;
  text-decoration: underline;
}

/* Values Section */
.values-section {
  background: linear-gradient(135deg, #fafbfc 0%, #f0f4f7 100%);
  padding: 4rem 0;
  margin-bottom: 0;
  opacity: 0;
  transform: translateY(40px);
  transition: all 1s ease-out;
}

.values-section.animate-in {
  opacity: 1;
  transform: translateY(0);
}

.values-section h2 {
  font-size: 2.5rem;
  font-weight: 800;
  color: #2d5016;
  text-align: center;
  margin: 0 0 3rem 0;
}

.values-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}

.value-item {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  padding: 2rem;
  background: white;
  border-radius: 16px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
  border: 1px solid rgba(74, 124, 35, 0.1);
  transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  opacity: 0;
  transform: translateX(-30px);
}

.value-item.stagger-child.animate-in {
  opacity: 1;
  transform: translateX(0);
}

.value-item:hover {
  transform: translateY(-6px) scale(1.02);
  box-shadow: 0 12px 40px rgba(74, 124, 35, 0.2);
}

.value-item:hover .value-icon {
  transform: scale(1.2) rotate(360deg);
  transition: transform 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.value-icon {
  font-size: 1.5rem;
  color: #4a7c23;
  min-width: 30px;
  margin-top: 0.25rem;
}

.value-content h3 {
  font-size: 1.25rem;
  font-weight: 700;
  color: #2d5016;
  margin: 0 0 0.5rem 0;
  line-height: 1.3;
}

.value-content p {
  color: #666;
  line-height: 1.5;
  margin: 0;
  font-size: 1rem;
}

/* FAQ Section */
.faq-section {
  background: white;
  padding: 4rem 0;
  margin-bottom: 0;
  opacity: 0;
  transform: translateY(40px);
  transition: all 1s ease-out;
}

.faq-section.animate-in {
  opacity: 1;
  transform: translateY(0);
}

.faq-section h2 {
  font-size: 2.5rem;
  font-weight: 800;
  color: #2d5016;
  text-align: center;
  margin: 0 0 3rem 0;
}

.faq-list {
  max-width: 800px;
  margin: 0 auto;
}

.faq-item {
  border-bottom: 1px solid #f0f4f7;
  margin-bottom: 0;
  opacity: 0;
  transform: translateY(20px);
  transition: all 0.6s ease-out;
}

.faq-item.stagger-child.animate-in {
  opacity: 1;
  transform: translateY(0);
}

.faq-question {
  width: 100%;
  padding: 1.5rem 0;
  background: none;
  border: none;
  text-align: left;
  cursor: pointer;
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 1.125rem;
  font-weight: 600;
  color: #2d5016;
  transition: color 0.3s ease;
}

.faq-question:hover {
  color: #4a7c23;
}

.faq-question:focus {
  outline: 2px solid #4a7c23;
  outline-offset: 2px;
}

.faq-toggle {
  font-size: 1.5rem;
  font-weight: 300;
  color: #4a7c23;
  transition: transform 0.3s ease;
}

.faq-question[aria-expanded="true"] .faq-toggle {
  transform: rotate(45deg);
}

.faq-answer {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease, padding 0.3s ease;
}

.faq-question[aria-expanded="true"] + .faq-answer {
  max-height: 200px;
  padding-bottom: 1.5rem;
}

.faq-answer p {
  color: #666;
  line-height: 1.6;
  margin: 0;
  font-size: 1rem;
}

/* Process Section */
.process-section {
  background: linear-gradient(135deg, #2d5016 0%, #1a2f0a 100%);
  color: white;
  padding: 4rem 0;
  margin-bottom: 0;
  position: relative;
  overflow: hidden;
  opacity: 0;
  transform: translateY(40px);
  transition: all 1s ease-out;
}

.process-section.animate-in {
  opacity: 1;
  transform: translateY(0);
}

.process-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.03'%3E%3Ccircle cx='30' cy='30' r='3'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
}

.process-section h2 {
  font-size: 2.5rem;
  font-weight: 800;
  text-align: center;
  margin: 0 0 3rem 0;
  position: relative;
  z-index: 2;
}

.process-steps {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 2rem;
  position: relative;
  z-index: 2;
}

.step {
  text-align: center;
  padding: 2rem 1rem;
  position: relative;
  opacity: 0;
  transform: scale(0.8);
  transition: all 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.step.stagger-child.animate-in {
  opacity: 1;
  transform: scale(1);
}

.step::after {
  content: '';
  position: absolute;
  top: 50%;
  right: -1rem;
  width: 2rem;
  height: 2px;
  background: linear-gradient(90deg, rgba(255,255,255,0.3), transparent);
  transform: translateY(-50%);
}

.step:last-child::after {
  display: none;
}

.step-icon {
  font-size: 3rem;
  margin-bottom: 1.5rem;
  display: block;
}

.step h3 {
  font-size: 1.375rem;
  font-weight: 800;
  margin: 0 0 1rem 0;
  line-height: 1.3;
}

.step p {
  font-size: 1rem;
  line-height: 1.6;
  opacity: 0.9;
  color: rgba(255, 255, 255, 0.95);
}

/* Contact Section */
.contact-section {
  background: linear-gradient(135deg, #fafbfc 0%, #f0f4f7 100%);
  padding: 4rem 0;
  margin-bottom: 0;
  opacity: 0;
  animation: fadeInUp 1s ease-out forwards;
  animation-delay: 1.2s;
}

.contact-section h2 {
  font-size: 2.5rem;
  font-weight: 800;
  color: #2d5016;
  text-align: center;
  margin: 0 0 3rem 0;
}

.contact-content {
  display: grid;
  grid-template-columns: 1fr 2fr;
  gap: 3rem;
  align-items: start;
}

.contact-info p {
  font-size: 1.125rem;
  color: #2c3e50;
  line-height: 1.7;
  margin-bottom: 2rem;
}

.direct-contact {
  background: white;
  padding: 2rem;
  border-radius: 16px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
  border: 1px solid rgba(74, 124, 35, 0.1);
}

.direct-contact h3 {
  font-size: 1.25rem;
  font-weight: 700;
  color: #2d5016;
  margin: 0 0 1rem 0;
}

.email-link {
  color: #4a7c23;
  text-decoration: none;
  font-weight: 600;
  font-size: 1.125rem;
  transition: color 0.3s ease;
}

.email-link:hover {
  color: #2d5016;
  text-decoration: underline;
}

.email-link:focus {
  outline: 3px solid #4a7c23;
  outline-offset: 2px;
}

/* Form Styles */
.contact-form-container {
  background: white;
  padding: 3rem;
  border-radius: 24px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
  border: 1px solid rgba(74, 124, 35, 0.1);
  position: relative;
  overflow: hidden;
  width: 100%;
  box-sizing: border-box;
  opacity: 0;
  transform: translateY(30px);
  animation: slideInUp 0.8s ease-out 1.5s forwards;
}

.contact-form-container::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, #4a7c23, #8bc34a);
}

.contact-form h3 {
  font-size: 2rem;
  font-weight: 800;
  color: #2d5016;
  margin: 0 0 2.5rem 0;
  text-align: center;
}

.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.5rem;
}

.form-group {
  margin-bottom: 2rem;
  opacity: 0;
  transform: translateY(20px);
  animation: slideInUp 0.6s ease-out forwards;
}

.form-group:nth-child(1) { animation-delay: 0.1s; }
.form-group:nth-child(2) { animation-delay: 0.2s; }
.form-group:nth-child(3) { animation-delay: 0.3s; }
.form-group:nth-child(4) { animation-delay: 0.4s; }
.form-group:nth-child(5) { animation-delay: 0.5s; }
.form-group:nth-child(6) { animation-delay: 0.6s; }

.form-group label {
  display: block;
  font-weight: 700;
  color: #2d5016;
  margin-bottom: 0.75rem;
  font-size: 1rem;
}

.form-group input,
.form-group select,
.form-group textarea {
  width: 100%;
  padding: 1.125rem 1.25rem;
  border: 2px solid #e5e7eb;
  border-radius: 12px;
  font-size: 1rem;
  transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  font-family: inherit;
  background: #fafbfc;
  box-sizing: border-box;
  position: relative;
}

.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
  outline: none;
  border-color: #4a7c23;
  background: white;
  transform: translateY(-2px) scale(1.02);
  box-shadow: 0 8px 30px rgba(74, 124, 35, 0.15), 0 0 0 3px rgba(74, 124, 35, 0.1);
}

.form-group textarea {
  resize: vertical;
  min-height: 120px;
}

.checkbox-group {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
}

.checkbox-label {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  font-size: 1rem;
  line-height: 1.6;
  color: #555;
  cursor: pointer;
}

.checkbox-label input[type="checkbox"] {
  width: auto;
  margin: 0;
  margin-top: 0.25rem;
  transform: scale(1.2);
}

.submit-btn {
  width: 100%;
  background: linear-gradient(135deg, #4a7c23 0%, #2d5016 100%);
  color: white;
  padding: 1.25rem 2rem;
  border: none;
  border-radius: 16px;
  font-size: 1.25rem;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  box-shadow: 0 4px 20px rgba(74, 124, 35, 0.3);
  position: relative;
  overflow: hidden;
  opacity: 0;
  transform: translateY(20px);
  animation: slideInUp 0.6s ease-out 0.7s forwards;
}

.submit-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
  transition: left 0.6s;
}

.submit-btn:hover::before {
  left: 100%;
}

.submit-btn:active {
  transform: translateY(-1px) scale(0.98);
  transition: all 0.1s ease;
}

.submit-btn:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 0 12px 40px rgba(74, 124, 35, 0.5);
}

.submit-btn:focus {
  outline: 3px solid #ffffff;
  outline-offset: 2px;
}

/* Footer CTA */
.footer-cta {
  background: linear-gradient(135deg, #2d5016 0%, #1a2f0a 100%);
  color: white;
  padding: 4rem 2rem;
  text-align: center;
  position: relative;
  overflow: hidden;
}

.footer-cta::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url("data:image/svg+xml,%3Csvg width='40' height='40' viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='rgba(255,255,255,0.03)' fill-rule='evenodd'%3E%3Cpath d='m0 40 40-40H20L0 20M40 40V20l-20 20'/%3E%3C/g%3E%3C/svg%3E");
  animation: float 20s ease-in-out infinite;
}

.cta-content {
  position: relative;
  z-index: 2;
}

.cta-content h2 {
  font-size: 2.5rem;
  font-weight: 800;
  color: white;
  margin: 0 0 1.5rem 0;
  line-height: 1.3;
}

.cta-content p {
  font-size: 1.25rem;
  color: rgba(255, 255, 255, 0.9);
  margin: 0;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
  line-height: 1.6;
}

/* Animation Keyframes */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(40px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideInScale {
  from {
    opacity: 0;
    transform: translateY(20px) scale(0.9);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@keyframes slideInLeft {
  from {
    opacity: 0;
    transform: translateX(-40px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes slideInRight {
  from {
    opacity: 0;
    transform: translateX(40px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes fadeInScale {
  from {
    opacity: 0;
    transform: scale(0.8);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

@keyframes pulse {
  0%, 100% {
    transform: scale(1);
  }
  50% {
    transform: scale(1.05);
  }
}

@keyframes shimmer {
  0% {
    transform: translateX(-100%);
  }
  100% {
    transform: translateX(100%);
  }
}

/* Typography and Spacing */
h1 {
  font-size: 3rem;
  line-height: 1.2;
}

h2 {
  font-size: 2.5rem;
  line-height: 1.3;
}

h3 {
  font-size: 1.5rem;
  line-height: 1.4;
}

p {
  font-size: 1rem;
  line-height: 1.6;
}

section {
  margin-bottom: 4rem;
}

@media (max-width: 768px) {
  .container {
    padding: 0 1rem;
  }

  .hero {
    min-height: 50vh;
  }
  
  .hero-content {
    grid-template-columns: 1fr;
    text-align: center;
    gap: 2rem;
    padding: 2rem 1rem;
  }
  
  .hero-text {
    padding-right: 0;
  }
  
  .hero-title {
    font-size: 2rem;
    line-height: 1.3;
  }
  
  .hero-subtitle {
    font-size: 1rem;
    max-width: none;
  }
  
  .jardinera-avatar {
    width: 80px;
    height: 80px;
    padding: 0.75rem;
  }
  
  .authority-stats {
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }
  
  .formats-grid {
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }
  
  .examples-grid {
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }
  
  .values-grid {
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }
  
  .process-steps {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  
  .step::after {
    display: none;
  }
  
  .contact-content {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  
  .authority-section,
  .formats-section,
  .examples-section,
  .values-section,
  .faq-section,
  .process-section,
  .contact-section {
    padding: 2rem 0;
  }
  
  .contact-form-container {
    padding: 1.5rem;
  }
  
  .form-group {
    margin-bottom: 1.5rem;
  }
  
  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: 1rem;
    font-size: 16px; /* Prevents zoom on iOS */
  }

  h1 {
    font-size: 2rem;
  }

  h2 {
    font-size: 2rem;
  }

  section {
    margin-bottom: 2rem;
  }
}

@media (max-width: 480px) {
  .hero {
    min-height: 50vh;
  }
  
  .hero-content {
    padding: 2rem 0.75rem;
    gap: 1.5rem;
  }
  
  .hero-title {
    font-size: 2rem;
    line-height: 1.2;
  }
  
  .hero-subtitle {
    font-size: 1rem;
  }
  
  .jardinera-avatar {
    width: 90px;
    height: 90px;
    padding: 1rem;
  }
  
  .about-section h2,
  .services-section h2,
  .partners-section h2,
  .process-section h2,
  .contact-section h2 {
    font-size: 2rem;
  }
  
  .contact-form-container {
    padding: 1.5rem;
    margin: 0 0.5rem;
    border-radius: 16px;
  }
  
  .contact-form h3 {
    font-size: 1.5rem;
  }
  
  .form-group {
    margin-bottom: 1.25rem;
  }
  
  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: 0.875rem;
    border-radius: 8px;
  }
  
  .submit-btn {
    padding: 1rem 1.5rem;
    font-size: 1.125rem;
  }
  
  .service-card,
  .partner-type {
    padding: 1.5rem;
  }
  
  .values-card,
  .contact-guidelines {
    padding: 1.5rem;
  }
  
  .container {
    padding: 0 0.75rem;
  }
  
  .about-section,
  .services-section,
  .partners-section,
  .process-section,
  .contact-section {
    padding: 2.5rem 0;
  }
}

/* Form Status Styles */
.form-status {
  padding: 1rem;
  border-radius: 8px;
  margin-bottom: 1.5rem;
  font-weight: 600;
  text-align: center;
}

.form-status.success {
  background: #d4edda;
  color: #155724;
  border: 1px solid #c3e6cb;
}

.form-status.error {
  background: #f8d7da;
  color: #721c24;
  border: 1px solid #f5c6cb;
}

.btn-loading {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
}

.btn-loading::after {
  content: '';
  width: 16px;
  height: 16px;
  border: 2px solid rgba(255, 255, 255, 0.3);
  border-radius: 50%;
  border-top-color: white;
  animation: spin 1s ease-in-out infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.submit-btn:disabled {
  opacity: 0.7;
  cursor: not-allowed;
  transform: none !important;
}
</style>

<script>
// URL de tu Google Apps Script Web App
const GOOGLE_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbwaY_g7hLY9UdKNGa4KAYx3B57frfe8Qq-xtGv15wGcUT4eJFMqYgNweHlwcs-II9d7/exec';

document.addEventListener('DOMContentLoaded', function() {
  // Intersection Observer for scroll-triggered animations
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('animate-in');
        
        // Add staggered animations for child elements
        const children = entry.target.querySelectorAll('.stagger-child');
        children.forEach((child, index) => {
          setTimeout(() => {
            child.classList.add('animate-in');
          }, index * 100);
        });
      }
    });
  }, observerOptions);

  // Observe sections for scroll-triggered animations
  const sectionsToObserve = document.querySelectorAll('.examples-section, .values-section, .process-section, .faq-section');
  sectionsToObserve.forEach(section => observer.observe(section));

  // FAQ Accordion functionality
  const faqQuestions = document.querySelectorAll('.faq-question');
  
  faqQuestions.forEach(question => {
    question.addEventListener('click', function() {
      const isExpanded = this.getAttribute('aria-expanded') === 'true';
      
      // Close all other FAQ items
      faqQuestions.forEach(q => {
        q.setAttribute('aria-expanded', 'false');
      });
      
      // Toggle current item
      this.setAttribute('aria-expanded', !isExpanded);
    });
  });

  // Form handling
  const form = document.getElementById('colaboraciones-form');
  if (!form) return;
  
  const submitBtn = document.getElementById('submit-btn');
  const btnText = submitBtn.querySelector('.btn-text');
  const btnLoading = submitBtn.querySelector('.btn-loading');
  const formStatus = document.getElementById('form-status');

  form.addEventListener('submit', async function(e) {
    e.preventDefault();
    console.log('Formulario enviado - iniciando proceso...');
    
    // Validar campos requeridos manualmente
    const requiredFields = form.querySelectorAll('[required]');
    let hasErrors = false;
    
    requiredFields.forEach(field => {
      if (!field.value.trim()) {
        field.style.borderColor = '#dc3545';
        hasErrors = true;
        console.log('Campo requerido vacío:', field.name);
      } else {
        field.style.borderColor = '#e5e7eb';
      }
    });
    
    if (hasErrors) {
      showError('Por favor, completa todos los campos obligatorios marcados con *');
      return;
    }
    
    // Mostrar estado de carga
    showLoading();
    console.log('Iniciando envío a Google Sheets...');
    
    // Recopilar datos del formulario
    const formData = new FormData(form);
    const data = Object.fromEntries(formData.entries());
    
    // Añadir timestamp
    data.timestamp = new Date().toLocaleString('es-ES', {
      timeZone: 'Europe/Madrid',
      year: 'numeric',
      month: '2-digit',
      day: '2-digit',
      hour: '2-digit',
      minute: '2-digit'
    });
    
    console.log('Datos a enviar:', data);
    
    try {
      console.log('Enviando a:', GOOGLE_SCRIPT_URL);
      
      // Intentar con URL params (método más compatible)
      const params = new URLSearchParams();
      Object.keys(data).forEach(key => {
        params.append(key, data[key]);
      });

      console.log('Parámetros a enviar:', params.toString());

      const response = await fetch(GOOGLE_SCRIPT_URL, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/x-www-form-urlencoded',
        },
        body: params.toString(),
        mode: 'no-cors'
      });
      
      console.log('Enviado con URLSearchParams');
      
      // Con no-cors no podemos leer la respuesta, asumimos éxito
      showSuccess('Recibido. Respondo en un máximo de 48 h.');
      form.reset();
    } catch (error) {
      console.error('Error completo:', error);
      showError('Hubo un problema al enviar tu mensaje. Error: ' + error.message + '. Por favor, inténtalo de nuevo.');
    } finally {
      hideLoading();
    }
  });
  
  function showLoading() {
    submitBtn.disabled = true;
    btnText.style.display = 'none';
    btnLoading.style.display = 'inline-flex';
    formStatus.style.display = 'none';
  }
  
  function hideLoading() {
    submitBtn.disabled = false;
    btnText.style.display = 'inline';
    btnLoading.style.display = 'none';
  }
  
  function showSuccess(message) {
    formStatus.textContent = message;
    formStatus.className = 'form-status success';
    formStatus.style.display = 'block';
    formStatus.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  }
  
  function showError(message) {
    formStatus.textContent = message;
    formStatus.className = 'form-status error';
    formStatus.style.display = 'block';
    formStatus.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
  }
});
</script>